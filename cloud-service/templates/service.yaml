apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-service.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cloud-service.labels" . | nindent 4 }}
spec:
  type: NodePort
  ports:
    - name: api
      port: 3000
      nodePort: {{ .Values.service.nodePorts.api }}
    {{- if .Values.service.nodePorts.nodeJSDebug}}
    - name: debug
      port: 9229
      nodePort: {{.Values.service.nodePorts.nodeJSDebug}}
  {{- end }}
  selector:
    {{- include "cloud-service.selectorLabels" . | nindent 4 }}
